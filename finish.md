# Ottimo lavoro! 🎉

Hai completato tutti i task di Mario. Il server è in ordine.

## Riepilogo di quello che hai fatto

| Task | Argomenti usati |
|------|----------------|
| 1 — Orientamento | `whoami`, `pwd`, `ls -la`, `cat`, navigazione filesystem |
| 2 — Permessi e directory | `chmod`, `mkdir`, `ls -l`, lettura notazione ottale |
| 3 — Analisi log | `grep`, `wc -l`, pipe `\|`, reindirizzamento `>` e `>>` |
| 4 — Sistema e rete | `free`, `df`, `ps aux`, `ip addr`, `ss`, `curl`, `ping` |
| 5 — Disco virtuale | `dd`, `mkfs.ext4`, `mount`, `umount`, `lsblk`, `du` |

## Quanto sei andato veloce?

- Sotto 20 minuti → sysadmin in erba 🚀
- 20–30 minuti → ben fatto, nella media 👍
- Oltre 30 minuti → ci vuole pratica, ma hai capito i concetti 💪

---

Mario ti manda un messaggio da Bali:

> *"Grazie! Sei il migliore. P.S. — la password del server è sempre quella di default, cambiala quando puoi."*

Alcune cose non cambiano mai.
